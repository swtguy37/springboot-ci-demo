pipeline {
    agent any

    tools {
        maven 'Maven'  // Name you gave in Jenkins Global Tool Configuration
    }

    stages {
        stage('Build') {
            steps {
                echo 'Build from Jenkins CI/CD'
                bat 'mvn clean install'
            }
        }
    }
}
